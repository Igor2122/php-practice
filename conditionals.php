<?php 
    $a = 5;
    $b = '5';
    $num = '10';
    
    $username = 'sketchings';
    //Available roles: author, editor, admin
    $role = 'editor';

    if($surname == '' && $role != 'admin'){
    echo "You do not have access to this page. Please contact your administrator.";
    }
    
    // if($num === 10 || is_string($num)) { // is_string will check if teh # is string yani
    //         echo "Your # is moore than 10 or is a string" ."\n";
    //     } else {
    //     echo "Your Number lower than 10 or it is not a string" ."\n";
    // }
    
    // if($num >= 10 && $num <= 1000) {
    //         echo "Your # is whithin the range" ."\n";
    //     } else {
    //     echo "Your Number is not within the range" ."\n";
    // }
    
    /* if($a != $b) {
    if($a) {
        echo "im am true anyway";
    }
    
        echo "values are not identical";
    } else {
        echo "you good";
    }
    
    */
    
    /* if($a == $b) {
         echo "Values are equal" . "\n";
     } else if($a < $b){
         echo "var a is less than var b" . "\n";
     } else if ($a > $b) {
         echo "var a i greater than var b" ."\n";
     } else {
         echo "Values are not eual" . "\n";
     }
     */
     
     
        
?>    